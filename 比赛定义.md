# ROGII — Wellbore Geology Prediction：比赛定义与冲金路线（定稿 v1）

生成时间：2026-06-02 · 依据：`eda.md`、`eda-2.md` + 原始数据/提交格式核对
Kaggle 账号：`/root/.kaggle-alt/kaggle.json`
状态：**未建模、未训练。本文件只定义问题、数据、难点、路线，不下任何模型结论。**

---

## 0. 一句话结论

那份建议是对的，而且和我们前三轮自己推出来的结论几乎完全一致——它把整盘棋收敛成了
**"先写 CV runner + baseline ladder，再跑 OOF surface + level-drift 判定，用结果决定怎么打"**。
本文件在它之上只做三处纠偏并补两条死线，其余照单全收。

三处纠偏：
1. **drift 是奖品，不是免费的 67.7%**——67.7% 是误差的*大小*，不是*可拿性*。
2. **formation surface 是待验证假设，不是已知主干**——且**测试集根本不给 formation tops**（见 §2），只能预测。
3. **worst-well 按 SSE（n_rows × RMSE²）排，不按 RMSE 排**——长井主导总分。

---

## 一、任务定义

**这不是表格回归，是序列外推。** 对每口水平井，从 prediction start（PS，`TVT_input` 开始缺失处）
向前重建一条**低频、连续、可升可降可平**的 `TVT`（True Vertical Thickness）曲线，直到井底。

- 已知段（prefix，`TVT_input` 非缺失）：造斜段 + 着陆段 + 一小段 lateral，含 X/Y/Z 轨迹与 GR。
- 隐藏段（post-PS，要预测）：约占每口井 **74%** 的行（全库 3,783,989 / 5,092,255 行）。
- 隐藏段 TVT 总跨度中位 **26.4 ft**、均值 29.4 ft（基本都在 5–50 ft 内，无小跨度井）。
- 物理上 TVT path 极光滑：对真值做 101-ft 平滑 RMSE 仅 **0.16**，401-ft 仅 0.95。

本质上，预测隐藏段 TVT = 预测这口井往前在地层里"长高/长低多少"，即 **level drift + 段内摆动**。

---

## 二、数据信息（已核对原始文件）

每口井两个 CSV，`{well_id}__horizontal_well.csv` 与 `{well_id}__typewell.csv`。

### 2.1 列结构（关键差异，决定整个策略）

| 文件 | train 列 | test 列 |
|---|---|---|
| `__horizontal_well.csv` | `MD,X,Y,Z, ANCC,ASTNU,ASTNL,EGFDU,EGFDL,BUDA, TVT, GR, TVT_input` | `MD,X,Y,Z, GR, TVT_input` |
| `__typewell.csv` | `TVT,GR,Geology` | `TVT,GR,Geology` |

> **致命要点：test 水平井不给 6 个 formation tops，也不给 TVT。**
> 推理时你手里只有 `MD,X,Y,Z,GR,TVT_input`（prefix 段有 TVT_input，post 段缺失）+ 该井 typewell。
> → formation surface **必须自己预测**，它在 test 上是隐变量，不是特征。这正是纠偏 #2 的硬证据。

### 2.2 规模
- train 井：**773**（773 horizontal + 773 typewell）。test 井：**3**（可见），hidden rerun 数量/分布未知。
- MD 步长 1 ft；typewell TVT 步长中位 0.5 ft（量化地板 0.144 ft，可忽略）。
- typewell `Geology` 含 6 个 formation top 同名标签 + 37 个额外细分标签（多数仅个位数井）。

### 2.3 提交格式
- `sample_submission.csv`：`id,tvt`，`id = {well_id}_{row_idx}`（row_idx 为该水平井 CSV 中 post-PS 缺失行的行号）。
- 每口可见 test 井提交行数 = 该井 post-PS 行数（000d7d20=3836，00bbac68=6014，00e12e8b=4301）。

### 2.4 可见 test 的真实地位
- 3 口可见 test 井**都在 train 里且有完整 TVT**（eda §6.1）→ **可见 test 只是 smoke test**。
- 无泄露可利用。那张 lookup 表降级为**调试用带标签验证样本**（确认对齐/surface 代码能复现真值），**不是策略**。

### 2.5 数据卫生
- 坐标干净（Z<0、X/Y 符号一致），无脏井可剔。
- typewell 有 **13 组逐字节重复（34 口井，最大组 10）**（eda §6.2）→ ① CV 分组必须把它们绑在一起防泄露；② 它们是现成的"影子邻井"。

---

## 三、评价与地板（误差结构）

度量：**per-row RMSE**（按行汇总 = row-weighted）。

| 量 | row-weighted RMSE | 含义 |
|---|---|---|
| Oracle-CF（常数=最后已知 TVT_input） | **15.91** | 安全地板 / baseline |
| Oracle-Const（常数=隐藏段真均值） | **9.04** | "估对 level" 的天花板，**不可达 oracle** |
| Oracle-Smooth-101 / 201 / 401 | 0.16 / 0.39 / 0.95 | 强平滑几乎免费 |

**CF 误差分解**：`MSE_CF = level_drift² + swing²`
- level_drift 分量 13.10（**67.7%** of MSE）
- swing 分量 9.04（32.3%）

**两条由此推出的硬事实：**
- 公开前沿 ~9.25–9.41 ≈ best-const oracle → **现有公开方案基本只做到"估对常数"，几乎没吃到 drift**。这就是金牌的机会窗口。
- per-well 均值 RMSE 12.8 < row-weighted 15.9 → **长井（行多、隐藏尾长、更难）主导总分**。
  → worst-well **按 SSE = n_rows × RMSE² 排序**（纠偏 #3）。一口 RMSE 30 × 10000 行的井，比 RMSE 70 × 400 行的井伤你多得多。

---

## 四、难点（按杀伤力排序）

1. **全程没有 LB 反馈，私榜一次性揭晓。** CV 是唯一导航；CV 错了没人戳穿你，危险翻倍。这是第一死线。
2. **drift 的可拿性未经验证。** 67.7% 的误差在 level drift，但要拿它＝预测井往前怎么走地层，几乎是整道题的重参数化。到现在**没人验证过现有特征能把 15.9 显著拉下来**。
3. **长尾十几口井主导金银差距。** worst CF 井 RMSE 到 70（隐藏段漂 90–120 ft），且这些井 self/typewell/GR 三信号同时失效（eda-2 §12）。金牌大概率取决于"这些井有没有翻车"。
4. **formation surface 在 test 是隐变量**（§2.1），主干假设必须先 OOF 验收才配当主干。
5. **三大信号都比直觉弱**（详见 §5）：self-prefix 不如随机、官方典井多数不是最像、GR 缺失且需仿射标定。
6. **hidden rerun 分布未知。** 可见 test 只有 3 口；未见过的井/典井、GR 大面积缺失、lateral 前缀≈0、孤岛井都要有兜底。

---

## 五、信号清单与可靠性分级（EDA 已量化）

| 信号 | 证据 | 可靠性 / 用法 |
|---|---|---|
| **carry-forward + 强平滑** | CF 15.9；smooth-201 oracle 0.39；残差 ACF 跌破 0.5 中位 406 ft | **安全地板**。后处理 ~200 ft 平滑代价 <0.4，几乎免费。退路就是它。 |
| **典井对齐（typewell）** | 隐藏段 100% 被典井 TVT 覆盖；同尺度可直接 join；量化地板 0.14 | **最强结构信号**。但 ↓ |
| └ 典井检索 | assigned 是 top1 仅 30/773、中位排名第 11；top1/top2 gap 中位 0.0007 | **弃用官方分配，全库检索**；**top-k 多候选软加权，禁 top1 贪心**。 |
| └ 典井标定 | corr~0.80；slope~0.80、intercept~17 | **仿射标定**（非平移），raw GR 不能直接比。 |
| **formation surface（邻井先验）** | Moran's I 0.84–0.89；nn 中位 478 ft、nn5 2290 ft；6 顶零交叉严格有序 | **待验证假设**。IDW/KNN 半径几千 ft 合理；但 test 不给 tops，**必须 OOF 预测 6 个面并先单独验收 RMSE**，准了才当主干。 |
| **GR** | post 缺失 31.7%，井间方差 3.8%→80%；最长 NaN 块 263 行；标定 slope 0.8 | **校正信号**，必须仿射标定 + 按 reliability/缺失率降权；缺失率本身做分流特征。 |
| **lateral 趋势外推** | 全 prefix Z 外推 RMSE 1259（含造斜段，灾难）；lateral-only 45.6 | **只用 lateral-only**，禁全前缀 Z/MD 外推；预测必须在 PS 边界接上 last-known TVT（锚点连续）。 |
| **self-prefix（本井前段）** | self NCC 0.38 < 随机对照 0.57；lateral 前缀中位仅 696 行、~20 井≈0 | **降级为带开关弱信号**，仅当 lateral 前缀够长且自相关显著时提权。任务书 slide 9 的"用本井前段"不可当普适第一信号。 |

---

## 六、原则（11 条 · 含 3 处纠偏，标【改】/【补】）

1. visible test 是 smoke test，hidden rerun 才是真任务；lookup 仅作调试验证样本，不投精力。
2. **CV 是唯一导航**：典井母本分组 + 空间压力分组 + 真 74% masked-tail。第一死线。
3. **【改】** 目标拆 level drift + smooth path；**drift 是奖品不是送分**，可拿多少 = surface/邻井先验的准度，而这个准度没人验证过。
4. **【改】** formation surface 必须先 OOF 验收再决定是否当主干（且 test 不给 tops，只能预测）。Moran's I 是值的空间自相关，≠ held-out 上预测得准。
5. GR 必须仿射标定（slope≈0.8、intercept≈17）+ reliability 降权，raw GR 不能直接比。
6. typewell top-k 多候选软加权，禁 top1 贪心；可检索全库最相似典井，不必只用官方分配。
7. self-prefix 只做带开关的弱信号。
8. 趋势只用 lateral-only，禁含造斜段全前缀外推；预测在 PS 边界锚点连续。
9. **【改】** worst-well **按 SSE（n_rows × RMSE²）排，不按 RMSE 排**。
10. **【补】知道何时躺平。** 三信号同时失效的长尾井上，错层对齐比 CF+平滑还差。"退回 CF+平滑（近免费安全地板）"是金牌级技能，贯穿所有分支。
11. **【补】hidden-safe fallback。** 对未见过的井/典井、GR 大面积缺失、lateral 前缀≈0、孤岛井都要兜底；固定种子、无外网、运行时可控。

---

## 七、冲金路线（4 步执行 · 别再扩原则）

> 纯思考的边际价值已为负（三轮 EDA + 五份策略，结论高度收敛）。卡点是执行圈数，不是想清楚没有。

**第 0 步 · 锁 CV runner（唯一基础设施）。**
三套 split（典井母本 / 空间压力 / well，重复典井组绑定）+ 真 74% masked-tail。
统一输出：row-weighted RMSE、per-well p50/p90/max、**按 SSE 排的 worst-well**、相对 CF 改善、按井型分桶。
**所有后续实验都走它。**

**第 1 步 · 钉 baseline ladder 当刻度。**
- B0 carry-forward（复现 ≈15.9）；B1 lateral-only 斜率；B2 检索典井对齐。
- O1 best-constant（9.04，**不可达 oracle**）；O2 smooth oracle。
- **第一天验收标准 = "level-drift 模型显著低于 15.9"，不是"破 9.04"。**

**第 2 步 · 跑那个唯一定生死的实验。**
- (a) **OOF formation-surface predictor**：先单独验收 6 个面在留出井上的 RMSE，且在典井/空间压力 CV 下稳不稳。
- (b) **level-drift 模型**：`target = hidden_mean − last_known`；特征用 PS 点坐标/轨迹/GR 可靠性/典井仿射标定/OOF surface/邻井先验。
  看 full RMSE 能从 15.9 降到多少、worst-SSE 井减没减、压力 CV 下还提不提升。

**第 3 步 · 看结果分叉（真正的战略决策，至今无人验证）：**
- **明显可降（→11、12 或更低）** → drift 可拿，金牌路线成立，再上 typewell top-k / 边界特征 / 平滑残差 / beam·PF / stack。
- **只小幅降（→14）** → 信号弱，转向保守 gating、高风险井退 CF、只在高置信井修正。
- **降不动 / 压力 CV 崩** → 现有特征拿不到 drift，游戏塌缩成"估稳 level + 绝不错层"，金牌改靠鲁棒 ensemble + 工程稳定。

---

## 八、第 3 步答案出来前 · 不要做的事

- 不要上 particle filter / beam search。
- 不要上大 stack / 复杂 ensemble。
- 不要继续复杂化系统、不要再写第六份策略。
- **先写 CV runner 和 ladder，再跑 OOF surface + level-drift 判定。** 是这个实验的结果（15.9 能不能显著降、压力 CV 撑不撑得住），而不是又一份原则，告诉你这块金牌怎么拿、甚至能不能拿。

---

## 九、实验已跑完 · 基础路线已约束死（v2，2026-06-02）

第 0–4 步全部执行完（`cv/cv_runner.py`、`cv/step1_3.py`、`cv/step4_dtw.py`，报告同目录）。结论把每条候选 drift 来源都量化封顶了：

| 候选 drift 来源 | 验证方式 | 结果 | 处置 |
|---|---|---|---|
| formation surface（主干假设） | oracle_surface_follow（用真 tops） | **108**，corr(tvt,top)≈-0.01，OOF spatial 407ft | **死**（oracle 证伪） |
| trajectory Z / TVD 校正 | corr + TVD-CF | corr≈-0.07，TVD-CF≈107 | **死** |
| spatial 邻井 drift kriging | neighbor_drift CV | spatial 16.6 > CF | **死** |
| typewell GR 约束对齐(DP/DTW) | Viterbi + 收缩 + 门控 | drift_corr 0.24，收缩后总 **15.56**（增益仅 0.35），物理封顶 | **弱**，可选低优先 |

**为什么 typewell 物理封顶**：lateral 段 `post_md_per_tvt≈200`，well 沿 MD 的 GR 变化由横向相变主导，typewell 只编码纵向 TVT 结构，两轴解耦 → GR 无法把 TVT 定到 ±几 ft。NCC 也救不了。

**刻度**：CF 15.91（地板）/ oracle_const 9.04（≈公开前沿，说明全场基本没人吃到 drift）/ oracle_smooth201 0.39（有真路径才拿得到的 shape 天花板）。

### 已约束的基础路线（建模起点）
1. **底座 = carry-forward（15.91），hidden-safe 兜底。** 绝对安全。
2. **swing 9.035 floor 暂不可触**：拿 shape 需可靠 TVT 路径，而我们没有；CF 是常数，平滑无效。
3. **唯一可加的真实信号 = typewell DP drift 收缩 β≈0.2**，仅值 ~0.35，带兜底，可选。
4. **彻底放弃**：surface / spatial / Z / PF / 大 stack。
5. **金牌的真正战场 = 长尾井不翻车 + 工程稳定 + 寻找本轮尚未触及的更强信号**（若存在），而非模型复杂度。这是一道"鲁棒地板 + 微弱 drift"的竞赛。

> 下一步若要继续找增益，方向只剩"本轮未触及的信号"：例如多典井 top-k 联合对齐、相邻水平井的 *已知 lateral 段* 直接迁移（而非典井）、或把 swing 当可预测结构再挖。但在找到 >0.35 的新证据前，先把 CF+兜底+收缩 nudge 的稳健提交管线搭好。
